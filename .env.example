# Airtable — used by app/api/leads/route.ts to store resume-download leads.
# Personal Access Token scoped to the base below, with data.records:write.
AIRTABLE_API_KEY=
# Base ID (starts with "app...").
AIRTABLE_BASE_ID=
# Table name within that base (e.g. "Resume Leads").
AIRTABLE_TABLE_NAME=
